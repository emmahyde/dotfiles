# Optional connector templates used by `mcx sync-connectors`.
# Replace each `example.invalid` URL with the endpoint for that MCP server before syncing.
# Entries are installed in `~/.claude.json` under these neutral aliases.
jira: https://jira.example.invalid/mcp
notion: https://notion.example.invalid/mcp
slack: https://slack.example.invalid/mcp
gdocs: https://gdocs.example.invalid/mcp
gsheets: https://gsheets.example.invalid/mcp
