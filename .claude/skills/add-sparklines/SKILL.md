---
name: add-sparklines
description: Add SPARKLINE formula columns to analysis tabs
---

Auto-generated by mcx skillsync from the "add-sparklines" chain; regenerated on every `mcx register`/`mcx remove`, so hand edits do not persist.

Add SPARKLINE formula columns to analysis tabs

Run it with the Bash tool, passing arguments as JSON:

    mcx run add-sparklines --args '{"...": "..."}'

Args schema:

```json
{
  "type": "object"
}
```
