<!-- BEGIN KIT FOOTER v1.3 -->

## Hard stops

- NEVER make fail test or check pass by weaken it — no skip, delete test, loosen assert, raise tolerance, widen catch block, `as any` / `# type: ignore`, lint-disable -> instead: quote fail, propose change, wait approval (silence check certify regression).
- NEVER run `git push` unless user ask for push this conversation — quote their word beside command -> instead: commit local, report (publish irreversible).
- NEVER kill process by image name (`taskkill /IM node.exe`, `pkill node`) -> instead: find PID via port (`lsof -ti :PORT` | `netstat -ano | findstr :PORT`) then kill that PID (image-name kill take down own harness).
- NEVER delete file/branch or run `git reset --hard` / `git checkout -- <file>` without paste what lost -> instead: paste exact target list, wait user approval this conversation (deletion unrecoverable).

After compaction or /resume: routing row 6 fire — write TRIGGER line, Read {{CLAUDE_DIR}}/guardrails/SESSION.md (S1 run first).

<!-- END KIT FOOTER -->
